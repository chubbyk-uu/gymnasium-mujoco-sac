# Verified versions (2026-06-18). Python 3.12.
gymnasium[mujoco]==1.3.0
mujoco==3.9.0
stable-baselines3==2.9.0
numpy==2.4.6
tensorboard==2.20.0
imageio==2.37.3          # used by enjoy.py --video

# PyTorch: install the build matching the target machine's CUDA (or CPU).
# This repo was tested on torch 2.12.0+cu130. On a new machine, prefer:
#   pip install torch --index-url https://download.pytorch.org/whl/cu130   (or cpu / your CUDA)
torch>=2.12
